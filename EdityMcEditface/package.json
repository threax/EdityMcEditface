{
  "name": "asp.net",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "@types/node": "^6.0.69"
  },
  "dependencies": {
    "editymceditface.client": "9.3.0"
  },
  "scripts": {
    "build": "node Build/build.js",
    "yarn-install": "yarn install --flat && yarn link threax-npm-tk",
    "clean": "powershell -Command Remove-Item 'ClientBin\\*' -Recurse -Force",
    "trash-modules": "powershell -Command Remove-Item 'node_modules\\*' -Recurse -Force",
    "import-tsconfig": "node node_modules\\threax-npm-tk\\tsimport.js"
  },
  "resolutions": {
    "htmlrapier": "8.0.4"
  }
}
