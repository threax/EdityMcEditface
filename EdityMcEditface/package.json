{
  "name": "asp.net",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "@types/node": "^6.0.69"
  },
  "dependencies": {
    "editymceditface.client": "8.0.0"
  },
  "scripts": {
    "build": "node Build/build.js",
    "yarn-install": "yarn install --flat",
    "link-tk": "yarn link threax-npm-tk",
    "clean": "powershell -Command Remove-Item 'ClientBin\\*' -Recurse -Force"
  }
}
